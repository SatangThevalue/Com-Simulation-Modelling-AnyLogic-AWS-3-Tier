นี่คือสรุปภาพรวมทั้งหมดของโปรเจกต์แบบจำลอง **SSO Core Application** ที่ถูกสร้างและปรับแต่งจนสมบูรณ์แบบบน AnyLogic 8.5.2 ครับ

### 🎯 วัตถุประสงค์ของโปรเจกต์

เพื่อจำลองสถาปัตยกรรม Microservices ของระบบประกันสังคม (SSO) และวิเคราะห์หาจุดคอขวด (Bottleneck) ที่ทำให้เกิดอาการระบบหน่วง (System Lag) หรือล่ม เมื่อมีผู้ใช้งานจำนวนมากแห่เข้ามาใช้งานพร้อมกันในช่วงเวลาเร่งด่วน (Peak Time)

---

### 🏗️ โครงสร้างสถาปัตยกรรมและการไหลของข้อมูล (Data Flow)

**1. Layer ผู้ใช้งาน (Client Agents)**

* **Agent:** `UserRequest` ที่มีตัวแปร `userType` (ประเภทผู้ใช้) และ `targetService` (ปลายทาง)
* **Traffic Control:** ใช้ Block `Schedule` กำหนดให้ผู้ใช้งานหลั่งไหลเข้ามาเฉพาะช่วง Peak (09:00 - 10:00 น.)
* **User Behavior:** Block `Source_Peak` จะสุ่มสร้างประเภทผู้ใช้งานตามสัดส่วนจริง ได้แก่ ผู้ประกันตน (INSURED), นายจ้าง (EMPLOYER) และ เจ้าหน้าที่ (STAFF)

**2. Layer ด่านหน้า (API Gateway)**

* **GatewayBuffer (Queue):** เป็นประตูด่านแรกที่รับความจุได้สูงสุด **500 Request** หากมีคนรอในคิวนานเกิน 30 วินาที ระบบจะตัดทิ้ง (Timeout) และหากคิวเต็ม 500 คนใหม่ที่เข้ามาจะถูกเด้งออกไปที่ Sink ทันที (จำลองเหตุการณ์ Server Overload / HTTP 503)
* **FastAPI_Process (Delay):** จำลองการประมวลผลของ API Gateway โดยเปิดโหมดประมวลผลแบบคู่ขนาน (Concurrent) ใช้โค้ด Java เช็ค `userType` เพื่อแปะป้ายหมายเลข `targetService` (1 ถึง 4) ให้กับ Request นั้นๆ

**3. Layer กระจายโหลด (Routing)**

* **Router (SelectOutput5):** ทำหน้าที่เป็นตัวสับราง นำ Request ที่ผ่านการประมวลผลจาก FastAPI มาแยกสายส่งไปยัง Microservices ทั้ง 4 ระบบอย่างแม่นยำ โดยไม่หลุดไปช่องทาง Error

**4. Layer บริการและฐานข้อมูล (Microservices & Database)** *จุดกำเนิด System Lag*

* **Microservices (Service Blocks):** แบ่งออกเป็น 4 ระบบ ได้แก่ ทะเบียน (Reg), เงินสมทบ (Contrib), ประโยชน์ทดแทน (Benefits) และบัญชี (Accounting) ซึ่งแต่ละระบบมีระยะเวลาประมวลผล (Delay time) ช้า-เร็ว แตกต่างกัน
* **Database (ResourcePool):** มีการจำกัดโควตา Connection สูงสุดไว้ที่ **100 เส้น** * **กลไกคอขวด:** ทุกครั้งที่ Microservices จะทำงาน ต้องมาดึง Connection จาก Database 1 เส้นเสมอ เมื่อโควตา 100 เส้นถูกแย่งกันใช้จนหมด Request ที่เหลือจะต้องกองรออยู่ที่คิวของ Service นั้นๆ ทำให้เห็นภาพ "ระบบหน่วง" ได้อย่างชัดเจน

**5. Layer การวัดผล (Analytics & Dashboard)**

* **Sinks:** มีจุดทิ้ง Request แยกตามประเภทเพื่อเก็บสถิติ เช่น ทำงานสำเร็จ (แยกตาม Service), หลุดเพราะ Timeout, หรือหลุดเพราะ Gateway รับไม่ไหว
* **Dashboard:** มีกราฟแบบ Real-time บนหน้าจอ
* **Bar Chart:** ดูจำนวน Request ที่ค้างรอ (Lag) อยู่ในแต่ละ Microservice
* **Time Plot:** ดูเส้นกราฟการดึงทรัพยากร Database ว่ากินไปกี่เปอร์เซ็นต์ และชนเพดาน 100 ตอนไหน



---

### ⚙️ จุดเด่นทางเทคนิคของการออกแบบนี้

1. **100% Native Java:** เปลี่ยนจากการพึ่งพาไลบรารี Pypeline และ Python มาเป็นการเขียน Logic ด้วย Java บน AnyLogic โดยตรง ทำให้โมเดลรันได้ด้วยความเร็วสูงสุด (Execution Speed) และหมดปัญหาเรื่อง Error ข้ามภาษาหรือเวอร์ชันไลบรารีไม่ตรงกัน
2. **Realistic Concurrency:** การจำลองฝั่ง API Gateway ถูกตั้งค่าให้ประมวลผลคนจำนวนมากพร้อมกันได้จริงๆ ไม่ใช่การต่อแถวทีละคน ทำให้คอขวดไปตกอยู่ที่ Database อย่างถูกต้องตามสถาปัตยกรรมของระบบ IT สมัยใหม่
3. **Fail-safe Logic:** โค้ดถูกเขียนมาให้ดักจับ Error ได้หมด ทั้งกรณีที่มีการสะกดคำผิด (พิมพ์ใหญ่/เล็ก) หรือกรณี Traffic ล้นจนทะลัก ก็จะถูกเก็บสถิติแยกไว้โดยไม่ทำให้โมเดลหยุดชะงัก (Crash) ระหว่างรัน